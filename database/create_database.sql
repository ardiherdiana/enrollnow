-- Create database for Evaluation System
-- Run this SQL script manually if the migration doesn't work

CREATE DATABASE IF NOT EXISTS `evaluation` 
CHARACTER SET utf8mb4 
COLLATE utf8mb4_unicode_ci;

-- Grant privileges (adjust username as needed)
-- GRANT ALL PRIVILEGES ON evaluation.* TO 'root'@'localhost';
-- FLUSH PRIVILEGES;

